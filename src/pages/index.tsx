import { useAppSelector } from '../store/hooks';

const Home = () => {
  // const { activeSong } = useA
  return (
    <div className="relative flex">
      <h1>Hello</h1>
    </div>
  );
};

export default Home;
